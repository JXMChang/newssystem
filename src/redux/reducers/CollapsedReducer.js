// TopHeader是发布者，SideMenu是订阅者
export const CollApsedReducer = (prevState = {
  isCollapsed:false
}, action) => {
  /* let {type} = action
  switch (type) {
    case 'value':
      
      return 'prevState';
  
    default:
      return prevState;
  } */
  return prevState;

}